{% extends 'base.html.twig' %}

{% block body %}
<article>
    <h2>{{ articls.Title }}</h2>
    <div class="metadata">Ecrit le {{ articls.createdAt | date }} à {{ articls.createdAt | date ('H:i')}}dans la categorie politique</div>
    <div class="content">
        <img src="{{ articls.image }}" alt="">
       {{ articls.content | raw }}
    </div>
</article>
{% endblock %}